---
title: Classic reference
nav_order: 10
---

# Classic reference — Medli gen1 (Medli-Classic)


*9 documented types.*

> **⚠️ Stale.** Auto-generated from the XML doc comments of the **Medli gen1**
> (Medli-Classic) source under `Medli-Classic/` (excluded from the build). The earliest
> Medli; here for historical reference only.


## `Medli`

- **`MIDE`** <small>(class, `Medli/Applications/IDE.cs`)</small> — Cocoapad Editor class contains methods needed for the editor to function
- **`Cpedit`** <small>(class, `Medli/Applications/cocoaedit.cs`)</small> — Cocoapad Editor class contains methods needed for the editor to function
- **`Cowsay`** <small>(class, `Medli/Applications/cowsay.cs`)</small> — A must-have for any command line operating system Doesn't have the other characters from the actual cowsay, but it'll do for now :P
- **`Mdscript`** <small>(class, `Medli/Applications/mdscript.cs`)</small> — Class for Medliscript (mdscript), a simple scripting language for the Medli command line shell
- **`GetHelp`** <small>(class, `Medli/Commands/commands/GetHelp.cs`)</small> — Help class, called by mshell
- **`Installer`** <small>(class, `Medli/Installer/Installer.cs`)</small> — Class for the Medli installer
- **`MEnvironment`** <small>(class, `Medli/Kernel/Environment.cs`)</small> — Will hold the environment methods which will be called by various components in Medli
- **`KernelVariables`** <small>(class, `Medli/Kernel/Variables.cs`)</small> — A class which holds the path of system directories in strings It also contains the method for creating these system directories.
- **`UserType`** <small>(enum, `Medli/UserManagement/Accounts.cs`)</small> — Create an account.
